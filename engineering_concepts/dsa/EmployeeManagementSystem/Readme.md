###Time Complexity
add-O(1)
search-O(n)
traverse-O(n)
delete-O(n)

*Limitations* of Arrays
1. They have fixed size and cannot grow dynamically
2. Deleting requires shifting of remaining elements
3. Searching takes linear time for unsorted arrays

*Used* when size is given and required to access using index